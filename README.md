# romance-novel-classifier
Binary text classifier for Project Gutenberg novels: predicts Romance (1) vs Non-Romance (0). Uses 1,800 novels per class, TF-IDF (uni+bi-grams), and an SGDClassifier (logistic regression). Achieves ~0.84 test accuracy and includes a CLI to classify new .txt files.
